# Project-local components live here. Import them in main.py
# BEFORE the workspace boots — the import triggers @register("...")
# so the scene yaml's ``type:`` lookup resolves to your class.
#
# Example:
#     from components.my_holder import MyHolder
#
# See docs/component-guide.md §3 "Where to put it" for the full
# pattern.

# printer_or/launch.yaml

scene: [scene/base.j2, scene/layout.j2]

kwargs:
  batch_size:
    type: int
    default: 1
    label: Batch size
    hint: Number of tubes to process
    min: 1
    max: 10

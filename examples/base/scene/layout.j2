# base — layout on the shared core_500 chassis.
#
# Empty on purpose: the base project is the bare chassis. Add your
# devices, tool racks and populated items here (see any other example's
# layout.j2 for the shape). Scene yaml rule: every field gets an
# explicit value — never commented-out optionals.
